# Infrastructure (Docker)
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
RABBITMQ_AMQP_PORT=5673
RABBITMQ_MANAGEMENT_PORT=15672
REDIS_PORT=6380

# SQL Server runs locally on your machine (not in Docker).
# Update connection strings in each service appsettings.json as needed.
